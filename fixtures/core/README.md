# Core fixtures

These records model a team-owned repository before and after the narrow APM remediation. They exercise matching and lifecycle logic only; they do not claim that the released `apm-cli` package executed.

`synthetic-canary.md` is visibly labelled test data with no personal, credential, or production content.
